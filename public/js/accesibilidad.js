import  AccesibilidadService  from './services/AccesibilidadService.js';
//public/js/accesibilidad.js

document.addEventListener('DOMContentLoaded', function () {
    console.log('cargo desde accesibilidad')
    // const toggleContrast = document.getElementById('toggle-contrast');
    // const increaseFont = document.getElementById('increase-font');
    // const decreaseFont = document.getElementById('decrease-font');
    // const toggleDarkMode = document.getElementById('toggle-dark-mode');
    // let currentFontSize = parseFloat(localStorage.getItem('fontSize')) || 16;

    // if (toggleContrast) {
    //     toggleContrast.addEventListener('click', () => {
    //         AccesibilidadService.toggleContrast();
    //         toggleContrast.setAttribute('aria-pressed', document.body.classList.contains('contrast-high'));
    //     });
    // }

    // if (increaseFont) {
    //     increaseFont.addEventListener('click', () => {
    //         currentFontSize = AccesibilidadService.increaseFont(currentFontSize);
    //     });
    // }

    // if (decreaseFont) {
    //     decreaseFont.addEventListener('click', () => {
    //         currentFontSize = AccesibilidadService.decreaseFont(currentFontSize);
    //     });
    // }

    // if (toggleDarkMode) {
    //     toggleDarkMode.addEventListener('click', () => {
    //         AccesibilidadService.toggleDarkMode();
    //     });
    // }

    // AccesibilidadService.loadPreferences();
});
